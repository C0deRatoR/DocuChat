version: '3.8'

services:
  docuchat:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - .:/app
      - faiss_index:/tmp/faiss_index
    env_file:
      - .env
    environment:
      - STREAMLIT_SERVER_PORT=8501
      - STREAMLIT_SERVER_ADDRESS=0.0.0.0

volumes:
  faiss_index:
